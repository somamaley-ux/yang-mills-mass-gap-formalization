\section{Phase-IX final technical-closure gate and audit boundary}\label{app:kernel-phase9}

This appendix records, from the proof-kernel surface, the final internal closure state reached at Phase~IX. It adds no mathematics and does not enlarge the kernel route. Its function is to tell a specialist reader exactly what the kernel can now be used for: direct attack on the live theorem route without reconstructive labor.

\paragraph{Kernel-facing summary.}
By Phase~IX the kernel now sits inside a synchronized package with four distinct layers: the live proof-home roots, this derived kernel extraction, the retained Phase~VIII adversarial-verification dossier, and the standalone Phase~IX closure-gate packet. Only the first layer carries proof burden.

\paragraph{Audit rule.}
Attack the kernel first. If a live theorem node needs more detail, descend only to the proof home cited for that node. Consult the Phase~IX packet only for the package-level pass conditions, theorem-node closure matrix, and final sign-off checklist. Reserve materials remain outside the proof burden.

\paragraph{Recorded completion state.}
The Phase~IX packet documents that the seam roles are synchronized, the import boundary is boxed, the endpoint framing matches the theorem exactly, and the blocker list inherited from the closure plan is now closed at the manuscript level. The standalone root \texttt{phase9\_closure\_gate.tex} is the canonical home for the full package-level record.
